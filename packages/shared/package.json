{
  "name": "@doubletake/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "zod schemas and TypeScript types shared by the server and the clients",
  "license": "AGPL-3.0-only",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/node": "^22.15.0"
  },
  "dependencies": {
    "ulid": "^3.0.2",
    "zod": "^4.5.4"
  }
}
