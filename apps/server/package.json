{
  "name": "@doubletake/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Doubletake server: channels, ingest queue, brains, media client, notifications, API",
  "license": "AGPL-3.0-only",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/node": "^22.15.0"
  }
}
